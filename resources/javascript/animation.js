/*
var StyleAnimator = Class.create(AnimationExecuter, {
  initialize: function(element, toStyle, time, method) {
    this.element = $(element);
    this.toStyle = toStyle;
    this.time = time;
  	this.method = method === null ? StyleAnimator.LINEAR;
  },
  
  callback: function(){
  	
  }
});
Object.extend(StyleAnimator, {
  LINEAR: 0,
  EASE: 1
});

var Animator = {
	AnimationQueue: new EventQueue(30),
	
	animateStyle: function(element, toStyle, time, method){
		new StyleAnimator($(element), toStyle, time, method);
	}

};
*/
