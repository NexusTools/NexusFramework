{
	"name": "ActionEmail",
	"provides": "action-email",
	"dependancies": ["mail-center"],
	"page-root": "/action-email/",
	"database": {
		"actions": {
			"code": "BINARY(16)",
			"callback": "VARCHAR(50)",
			"arguments": "BINARY(2048)",
			"single-use": {
				"type": "BOOLEAN",
				"default": 1
			},
			"expires": "TIMESTAMP"
		}
	}
}
