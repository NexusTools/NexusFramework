{
	"name": "MOE Game Server",
	"provides": "moe-game-server",
	"description": "A API to manage MOE Game Servers",
	"dependancies": ["phpcomm"],
	"database": {
		"servers": {
			"name": "VARCHAR(20)",
			"auth": "BINARY(16)",
			"host": "VARCHAR(20)",
			"port": "INTEGER"
		}
	}
}
